version: 1
workspace:
  id: a96d2d7cef9d481fa47738e63ff316ed
  name: nitroci-plugin-bitbucket
commands:
  - name: build
    description: Build the command line.
    steps:
      - cwd: .
        scripts: 
          - mkdir -p ./dist && go build -o ./dist .
