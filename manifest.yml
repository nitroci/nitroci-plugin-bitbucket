id: 218d3caf07424b10a145ee5e1f036174
name: bitbucket
operations:
    configure:
        enabled: true
        flags:
            - name: bitbucket-workspace
              shorthand:
              type: string
              usage: bitbucket workspace
            - name: bitbucket-user
              shorthand:
              type: string
              usage: bitbucket user
            - name: bitbucket-pass
              shorthand:
              type: string
              usage: bitbucket password
    environments:
        enabled: true
    pipelines:
        enabled: true